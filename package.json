{
    "name": "jetli",
    "version": "0.0.1",
    "description": "Dependency injector",
    "main": "dist/cjs/index.js",
    "module": "dist/esm/index.js",
    "typings": "dist/esm/index.d.ts",
    "scripts": {
        "// DEV SCRIPTS //": "-------------------------------------------------------",
        "watch": "npm-watch build",
        "build": "run-s -s clean build:esm build:cjs",
        "build:esm": "tsc -p tsconfig.esm.json",
        "build:cjs": "tsc -p tsconfig.cjs.json",
        "// CODE QUALITY SCRIPTS //": "----------------------------------------------",
        "lint": "tslint -c tslint.json src/**/*.ts",
        "test": "jest --config jest-unit.config.json",
        "e2e": "jest --config jest-e2e.config.json",
        "test:watch": "jest --config jest-unit.config.json --watch",
        "test:coverage": "jest --config jest-unit.config.json --coverage",
        "// UTILS SCRIPTS //": "-----------------------------------------------------",
        "docs": "run-s docs:html docs:md",
        "docs:html": "typedoc --mode modules --exclude \"**/*+(index|.spec|.e2e).ts\" --tsconfig tsconfig.esm.json --hideGenerator --readme src/docs/README.html --out docs src",
        "docs:md": "typedoc --mode modules --exclude \"**/*+(index|.spec|.e2e).ts\" --tsconfig tsconfig.esm.json --hideGenerator --readme src/docs/README.html --theme markdown --mdEngine github --out docs src",
        "clean": "rimraf dist && rimraf docs && rimraf site && rimraf coverage"
    },
    "watch": {
        "build": {
            "patterns": [
                "src"
            ],
            "ignores": [
                "**/*.d.ts"
            ],
            "extensions": "ts",
            "quiet": false
        }
    },
    "dependencies": {},
    "devDependencies": {
        "@types/jest": "24.0.12",
        "husky": "2.2.0",
        "jest": "24.7.1",
        "npm-run-all": "4.1.5",
        "npm-watch": "0.6.0",
        "rimraf": "2.6.3",
        "ts-jest": "24.0.2",
        "tslint": "5.16.0",
        "typedoc": "0.14.2",
        "typescript": "3.4.5",
        "typedoc-plugin-markdown": "1.2.0"
    },
    "engines": {
        "node": ">=6.11.5"
    },
    "author": "Filip Dabrowski",
    "repository": {
        "type": "git",
        "url": "git@github.com:thefill/interject.git"
    },
    "license": "MIT"
}
